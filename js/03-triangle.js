//  triangle limitter, start point and the output
let triangleNum = 6;
let i = 0;
let output = ""

do {
    //  accumulate the output and log it to the console while the condition is true
    output += "#"
    console.log(output)
    i++
    //  continue to do this while i is less than 6
} while (i < triangleNum)